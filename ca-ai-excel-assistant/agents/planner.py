"""
Planner agent — extracts intent, dates, clientTag, risk flag from user query.
No AI logic yet (Step 1 skeleton).
"""


def plan(query: str) -> dict:
    """Placeholder: returns empty structured output."""
    return {"intent": "", "dates": [], "client_tag": None, "risk_flag": False}
