"""
Normalize column names, dates (ISO), amounts (numbers).
Step 1 skeleton — placeholder only.
"""
# import pandas as pd  # Uncomment when implementing Step 4


def normalize(df) -> "tuple":
    """
    Placeholder: normalize DataFrame (columns, dates, amounts).
    Returns (None, []) until Step 4.
    """
    return None, []
