# Step 11: Reference for Streamlit Community Cloud secrets.
# Do NOT commit real values. In Cloud: Dashboard → App → Settings → Secrets.
# These keys are also read via python-dotenv (.env) locally and as env vars on Cloud.

# MongoDB Atlas (required for saving data and chat)
# MONGODB_URI = "mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/DB?retryWrites=true&w=majority"
# MONGODB_DB_NAME = "ca_ai_excel"

# Groq (required for full LLM behavior; app works with fallbacks if missing)
# GROQ_API_KEY = "gsk_..."
# GROQ_MODEL = "llama-3.3-70b-versatile"

# ChromaDB (optional; default persist dir used if not set)
# CHROMA_PERSIST_DIR = "./chroma_db"
